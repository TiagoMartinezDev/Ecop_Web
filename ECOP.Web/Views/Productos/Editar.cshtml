@model ProductoFormViewModel
@{ ViewData["Title"] = "Editar Producto"; }

<div class="d-flex align-items-center mb-3 gap-2">
    <a asp-action="Index" class="btn btn-outline-secondary btn-sm"><i class="bi bi-arrow-left"></i></a>
    <h2 class="mb-0"><i class="bi bi-pencil-square me-2"></i>Editar Producto</h2>
</div>

<div class="card shadow-sm" style="max-width:600px">
    <div class="card-body">
        <form asp-action="Editar" asp-route-id="@Model.Id" method="post">
            @Html.AntiForgeryToken()
            <input type="hidden" asp-for="Id" />
            <div class="mb-3">
                <label class="form-label fw-semibold">Código</label>
                <input value="@Model.Codigo" class="form-control bg-light" readonly />
                <input type="hidden" asp-for="Codigo" />
                <small class="text-muted">El código no puede modificarse.</small>
            </div>
            <div class="mb-3">
                <label asp-for="Descripcion" class="form-label fw-semibold"></label>
                <input asp-for="Descripcion" class="form-control" />
                <span asp-validation-for="Descripcion" class="text-danger small"></span>
            </div>
            <div class="mb-3">
                <label asp-for="UnidadMedidaId" class="form-label fw-semibold"></label>
                <select asp-for="UnidadMedidaId" asp-items="Model.UnidadesMedida" class="form-select">
                    <option value="">-- Seleccionar --</option>
                </select>
                <span asp-validation-for="UnidadMedidaId" class="text-danger small"></span>
            </div>
            <div class="row">
                <div class="col-md-6 mb-3">
                    <label asp-for="PrecioUnitario" class="form-label fw-semibold"></label>
                    <div class="input-group">
                        <span class="input-group-text">Gs.</span>
                        <input asp-for="PrecioUnitario" class="form-control text-end" />
                    </div>
                    <span asp-validation-for="PrecioUnitario" class="text-danger small"></span>
                </div>
                <div class="col-md-6 mb-3">
                    <label asp-for="Stock" class="form-label fw-semibold"></label>
                    <input asp-for="Stock" class="form-control text-end" />
                    <span asp-validation-for="Stock" class="text-danger small"></span>
                </div>
            </div>
            <div class="d-flex gap-2 mt-2">
                <button type="submit" class="btn btn-warning">
                    <i class="bi bi-save me-1"></i>Actualizar
                </button>
                <a asp-action="Index" class="btn btn-outline-secondary">Cancelar</a>
            </div>
        </form>
        <form asp-action="CambiarActivo" asp-route-id="@Model.Id" method="post" class="mt-2">
            @Html.AntiForgeryToken()
            <button type="submit" class="btn btn-outline-success @(Model.Activo ? "d-none" : "") ">
                <i class="bi @(Model.Activo ? "bi-toggle-on" : "bi-toggle-off") me-1"></i>
                Activar
            </button>
        </form>
    </div>
</div>

@section Scripts {
    <partial name="~/Views/Shared/_ValidationScripts.cshtml" />
}
